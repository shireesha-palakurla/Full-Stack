// Importing specific functions from the 'math.js' module
import { add, diff, dif, mult } from './math.js';   

// Calling the 'add' function with arguments 20 and 30, and logging the result to the console
console.log(add(20, 30)); // Expected output: 50

// Calling the 'diff' function with arguments 5 and 1, and logging the result to the console
console.log(diff(5, 1)); // Expected output: 4

// Calling the 'dif' function with arguments 3 and 15, and logging the result to the console
console.log(dif(3, 15)); // Expected output: -12 (assuming 'dif' calculates the difference)

// Calling the 'mult' function with arguments 4 and 5, and logging the result to the console
console.log(mult(4, 5)); // Expected output: 20
